import 'package:flutter/material.dart';

class OverallStatisticsScreen extends StatelessWidget {
  const OverallStatisticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Profile Statistics'),
      ),
    );
  }
}
